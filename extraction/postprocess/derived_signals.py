from __future__ import annotations

from typing import Any


def apply_derived_signals(signals: list[dict[str, Any]]) -> list[dict[str, Any]]:
    """Minimal deterministic derivations; conservative and evidence-backed only."""
    return signals
